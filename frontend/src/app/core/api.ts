// Base URL of the Express API. At deploy time this moves to Angular environment
// files so production points at the hosted backend instead of localhost.
export const API_BASE_URL = 'http://localhost:5000';
